#include <stdio.h>
int main(){
    FILE *src=fopen("input.txt","r"),*dst=fopen("output.txt","w");
    char c; if(!src||!dst){ printf("File error\n"); return 0; }
    while((c=fgetc(src))!=EOF) fputc(c,dst);
    printf("Copied successfully\n");
    fclose(src); fclose(dst);
}
OUTPUT:
File error
